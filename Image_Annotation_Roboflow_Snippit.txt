COCO
!pip install roboflow

from roboflow import Roboflow
rf = Roboflow(api_key="8O0d3piVDubPgWzft4ha")
project = rf.workspace("bdbi").project("image-annotation-lxsso")
dataset = project.version(5).download("voc")


curl -L "https://app.roboflow.com/ds/Dtps0rgBT7?key=PYMgq53qLg" > 

Tensorflow

!pip install roboflow

from roboflow import Roboflow
rf = Roboflow(api_key="8O0d3piVDubPgWzft4ha")
project = rf.workspace("bdbi").project("image-annotation-lxsso")
dataset = project.version(5).download("tensorflow")


!pip install roboflow

from roboflow import Roboflow
rf = Roboflow(api_key="8O0d3piVDubPgWzft4ha")
project = rf.workspace("bdbi").project("image-annotation-lxsso")
dataset = project.version(5).download("tfrecord")

https://github.com/tensorflow/tensorboard/issues/3186
